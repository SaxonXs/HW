[README]

The program works with heart.csv dataset from Andrew's git account
For this program to work, you will need to insert all the info about your case in either of entrypoints file.

entrypoint_forest.py applies random forest model.
entrypoint_regression.py applies logistic regression model.